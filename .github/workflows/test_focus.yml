name: Test Focus API

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test Focus IPCA API
        run: python3 test_focus.py
